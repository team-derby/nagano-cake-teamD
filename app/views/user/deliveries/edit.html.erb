<div class="contaier">
  <div class="row">
    <h2><span style="background-color: #F5F5F5;">配送先編集</span></h2>
    <!-- フラッシュメッセージ -->
    <p id="notice"><%= notice %></p>
      <%= form_for @delivery, url: user_user_delivery_path(user_id: current_user.id, id: @delivery.id), method: :patch do |f| %>
      <table style="border:none;" width="600" height="150">
        <!-- エラーメッセージ -->
        <% if @delivery.errors.any? %>
        <div id="error_explanation">
          <h3><%= pluralize(@delivery.errors.count, "error") %> prohibited this product from being saved:</h3>

          <ul>
          <% @delivery.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>
        <tr>
          <th><%= f.label :'郵便番号(ハイフンなし)' %></th>
          <td><%= f.text_field :post_number, class: "post_numer" %></td>
        </tr>
        <tr>
          <th><%= f.label :'住所' %></th>
          <td><%= f.text_field :post_address, class: "post_address" %></td>
        </tr>
        <tr>
          <th><%= f.label :'宛名' %></th>
          <td><%= f.text_field :post_name, class: "post_name" %></td>
          <td><%= f.submit "編集する", class: 'btn btn-success btn-md' %></td>
        </tr>
      </table>
    <% end %>
  </div>
</div>